import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.exptrackerdmytruk.app',
  appName: 'Expense tracker',
  webDir: 'www'
};

export default config;
